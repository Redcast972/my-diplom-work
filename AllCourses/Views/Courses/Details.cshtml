@using AllCourses.Domain.Entites.Courses
@model CourseEntity
@{
    Layout = "_Layout";
    ViewData["Title"] = Model.Title;
}

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">


    <div class="bg-white p-6 rounded-lg shadow-md">
        <div class="relative">
            @if (Model.ImageData != null)
            {
                <img src="data:image/png;base64,@Convert.ToBase64String(Model.ImageData)" alt="Новость" class="rounded-lg shadow-md w-full h-full object-cover" />
            }
            else
            {
                <p class="text-gray-500 mt-2">Нет изображения</p>
            }
        </div>

        <h2 class="text-3xl font-bold text-gray-600 mb-6">@Model.Title</h2>

        <div class="mt-4">
            <p class="text-gray-600 mt-4">@Model.Discription</p>
            <p class="text-sm text-gray-500">Дата публикации: @Model.CreatedAt.ToString("dd.MM.yyyy")</p>
        </div>

        <!-- Кнопка для возврата или перехода -->
        <div class="mt-6">
            <a href="/teaching" class="text-indigo-600 hover:text-indigo-800 font-semibold">
                Вернуться назад
            </a>
        </div>
    </div>

    <div class="bg-white p-6 rounded-lg shadow-md mt-10">
        <h2 class="text-2xl font-bold text-gray-600 mb-6">Уроки</h2>
        @if (Model.LessonsId.Count == 0)
        {
            //TODO: Придумать как сюда впихнуть айдишник курса
            <a href="/courses/Details/{id}/addlesson" class="bg-indigo-600 text-white px-6 py-3 rounded-lg shadow-md hover:bg-indigo-700 transition">
                Добавить урок
            </a>
        }
    </div>

    <div class="bg-white p-6 rounded-lg shadow-md mt-10">
        <h2 class="text-2xl font-bold text-gray-600 mb-6">Тесты</h2>
        @if (Model.LessonsId.Count == 0)
        {
            //TODO: Придумать как сюда впихнуть айдишник курса
            <a href="/courses/Details/{id}/addtest" class="bg-indigo-600 text-white px-6 py-3 rounded-lg shadow-md hover:bg-indigo-700 transition">
                Добавить тест
            </a>
        }
    </div>
</div>